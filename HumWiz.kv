Final: 

<Final>:
    TextInput:
        id: input
        font_size: 20
        height: 100
        size_hint_y: None  
        pos: (0, 2000)
    Label:
        id: final_display
        text: str("The key is either " + root.major + " major or " +  root.minor + " minor")
        font_size: "30dp"
        size_hint: .25, .25
        pos_hint: {"center_x": .5, "center_y": 100}
    Label:
        id: title
        text: "Welcome to HumWiz!"
        size_hint: .25, .25
        font_size: "60dp"
        pos_hint: {"center_x": .5, "center_y": .75}
    Label:
        id: watch
        text: str(round(root.number))
        font_size: "50dp"
        pos: 0, 50000
    Button:
        id: stop_input
        text: "Enter"
        on_press: root.stop_input()
        size_hint: 1, .25
        font_size: "25dp"
        pos: 0, 5000
    Label:
        id: Recording
        text: "Recording..."
        size_hint: .25, .25
        font_size: "50dp"
        pos_hint: {"center_x": .5, "center_y": 100}
    Label:
        id: input_instructions
        text: "Copy the file location"
        size_hint: .25, .25
        font_size: "50dp"
        pos_hint: {"center_x": .5, "center_y": 100}
    
    BoxLayout:
        orientation: "horizontal"
        spacing: "50dp"
        Button:
            id: but_input
            disabled: False
            text: "Input File"
            font_size: "25dp"
            on_press: root.on_button_click()
            size_hint: .25, .25
            pos_hint: {"bottom": .5}
        Button:
            id: but_record
            disabled: False
            text: "Record a 15-sec snippet"
            on_press: root.before_record()
            size_hint: .25, .25
            font_size: "25dp"
            pos_hint: {"bottom": .5}
    
